name: 'my_new_project'
version: '1.0.0'
config-version: 2

profile: 'my_new_project'

model-paths: ["models"]
macro-paths: ["macros"]
seed-paths: ["seeds"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  my_new_project:

    +persist_docs:
      relation: true
      columns: true

    # Default
    +materialized: view